# ============ 依赖与构建产物 ============
node_modules/
frontend/dist/
__pycache__/
*.pyc
build_release/

# ============ 运行产物 ============
logs/
*.log
# 本地/测试环境版本标记（打包版由构建脚本写入自己目录的 version.txt）
version.txt
# 地牢运行时存档（run_state/进度，按需 gitignore；不传远端）
data/

# ============ 隐私与密钥（绝不入库） ============
# 含 API 密钥与个人安全配置
config/config.yaml
# 含个人角色设定（NSFW）
config/character.yaml
# 配件与强度基准（NSFW）
config/device_channels.yaml
# 风格版本/昵称运行时覆盖
config/character_runtime.yaml
# 配置文件备份
*.bak
.env
*.pem
#webview_storage保存路径
config/webview_storage

# ============ 私有内容包与生产工具（不上传） ============
# 调教版提示词/语料（NSFW，私发内容包）
content/pack/
# 蒸馏/反推等生产工具
tools/

# ============ 个人素材与本地大模型 ============
素材/
ollama/

# ============ IDE ============
.vscode/
.idea/
# ===== public-push isolation (2026-09-03): NSFW role content & dungeon story assets stay local =====
# R18 内容一律不随主仓库/主发布包分发，只走 DLC 大包（zh/en）由用户自行导入
content/roles/
content/pure/
content/pack/
backend/story_pack/
_t032_*
_t048_*
_t058_*
_t061_*
_t063_*
